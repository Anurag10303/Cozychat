# Backend API base URL.
# Optional in production when the backend serves this app (same origin is used).
VITE_API_URL=http://localhost:3000

# Optional. Google sign-in reads GOOGLE_CLIENT_ID from the backend at runtime,
# so you normally leave this empty. Set it only to override at build time.
VITE_GOOGLE_CLIENT_ID=
